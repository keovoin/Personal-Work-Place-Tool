{
  "name": "productivity-dashboard-web",
  "version": "1.0.0",
  "private": true,
  "description": "Hosted web/PWA version of the Hybrid Productivity Dashboard. Reuses the pure domain core; uses IndexedDB and manual + idle-aware time tracking.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "build:nocheck": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "idb": "^8.0.0"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vite": "^5.4.11",
    "vite-plugin-pwa": "^0.21.1"
  }
}
